global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'drift-bots'
    static_configs:
      - targets: ['host.docker.internal:9109']  # Your metrics port (using host.docker.internal for Docker)
    scrape_interval: 5s
    metrics_path: /metrics