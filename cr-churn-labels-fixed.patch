*** a/run_swift_mm_complete.py
--- b/run_swift_mm_complete.py
@@ -2293,6 +2293,35 @@ class CompleteSwiftMMBot:
     async def cancel_replace_order(self, order_id: str, side: str, price: float, size: float) -> Optional[str]:
         """
         Cancel existing order and place new order atomically
         Implements emulated cancel/replace with idempotency for immediate deployment
         """
+        # Enhanced C/R with churn correlation labels
+        cancel_reason = getattr(order_id, 'cancel_reason', 'manual') if hasattr(order_id, '__dict__') else 'manual'
+        cancel_alignment = getattr(order_id, 'cancel_alignment', 'n/a') if hasattr(order_id, '__dict__') else 'n/a'
+
+        # Record C/R execution with inherited labels
+        try:
+            # Log the C/R with correlation labels
+            logger.info(f"🔄 Cancel/Replace: {order_id} → {side} {size:.4f} @ ${price:.2f}")
+            logger.info(f"   Reason: {cancel_reason}, Alignment: {cancel_alignment}")
+
+            # Phase 1: Cancel existing order
+            cancel_success = await self._cancel_order_via_sidecar(order_id)
+            if cancel_success:
+                logger.info(f"✅ Cancelled order: {order_id}")
+            else:
+                logger.warning(f"⚠️  Cancel failed for {order_id}, proceeding with new order")
+
+            # Phase 2: Place new order with idempotency key
+            # Use order_id + timestamp for idempotency to prevent duplicates
+            import time
+            idempotency_key = f"cr-{order_id}-{int(time.time()*1000)}"
+
+            new_order_id = await self._place_order_via_sidecar(side, price, size)
+
+            if new_order_id:
+                logger.info(f"✅ Cancel/Replace completed: {order_id} → {new_order_id}")
+
+                # Update order tracking
+                if order_id in self.active_orders:
+                    old_order = self.active_orders.pop(order_id)
+                    logger.info(f"📊 Replaced {old_order.side} @ ${old_order.price:.2f} with {side} @ ${price:.2f}")
+
+                return new_order_id
+            else:
+                logger.error(f"❌ Cancel/Replace failed: could not place new order")
+                return None
+
+        except Exception as e:
+            logger.error(f"❌ Cancel/Replace error: {e}")
+            return None

*** a/libs/metrics.py
--- b/libs/metrics.py
@@ -1,22 +1,70 @@
 #!/usr/bin/env python3
 """
 Simple metrics module for bot monitoring
 """
 
 from prometheus_client import start_http_server, Gauge, Counter, Histogram
+import logging
+from typing import Dict, Any, Optional
+
+logger = logging.getLogger(__name__)
 
 # Start metrics server
 def start_metrics(port: int = 9100):
     """Start Prometheus metrics server"""
     start_http_server(port)
 
 # Bot metrics
-LOOP_LATENCY = Histogram('bot_loop_latency_seconds', 'Bot loop execution time')
-ORDERS_PLACED = Counter('orders_placed_total', 'Total orders placed')
-INV_USD = Gauge('inventory_usd', 'Current inventory in USD')
+LOOP_LATENCY = Histogram('bot_loop_latency_seconds', 'Bot loop execution time')
+ORDERS_PLACED = Counter('orders_placed_total', 'Total orders placed')
+INV_USD = Gauge('inventory_usd', 'Current inventory in USD')
+
+# Enhanced C/R metrics with labels
+CANCEL_REPLACE_TOTAL = Counter(
+    'cancel_replace_total',
+    'Cancel→Replace lifecycle with labels for churn correlation',
+    ['phase', 'alignment', 'reason', 'via', 'result']
+)
+
+class EnhancedMetrics:
+    """Enhanced metrics class with label support"""
+
+    def __init__(self):
+        self._counters = {}
+
+    def inc(self, name: str, labels: Optional[Dict[str, str]] = None):
+        """Increment counter with optional labels"""
+        try:
+            if labels:
+                # Use Prometheus Counter with labels
+                if name == 'cancel_replace_total':
+                    CANCEL_REPLACE_TOTAL.labels(
+                        phase=labels.get('phase', 'unknown'),
+                        alignment=labels.get('alignment', 'n/a'),
+                        reason=labels.get('reason', 'unknown'),
+                        via=labels.get('via', 'unknown'),
+                        result=labels.get('result', 'unknown')
+                    ).inc()
+                else:
+                    logger.warning(f"Unknown counter with labels: {name}")
+            else:
+                # Fallback to global counters
+                if name == 'orders_placed_total':
+                    ORDERS_PLACED.inc()
+                else:
+                    logger.warning(f"Unknown counter: {name}")
+        except Exception as e:
+            logger.warning(f"Failed to increment counter {name}: {e}")
+
+    def observe(self, name: str, value: float, labels: Optional[Dict[str, str]] = None):
+        """Observe histogram with optional labels"""
+        try:
+            if name == 'bot_loop_latency_seconds':
+                LOOP_LATENCY.observe(value)
+            else:
+                logger.warning(f"Unknown histogram: {name}")
+        except Exception as e:
+            logger.warning(f"Failed to observe {name}: {e}")
+
+    def set(self, name: str, value: float):
+        """Set gauge value"""
+        try:
+            if name == 'inventory_usd':
+                INV_USD.set(value)
+            else:
+                logger.warning(f"Unknown gauge: {name}")
+        except Exception as e:
+            logger.warning(f"Failed to set gauge {name}: {e}")
 
 # Market metrics
 def record_mid(price: float):




